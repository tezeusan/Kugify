apiVersion: v1
kind: Service
metadata:
  name: {{ include "kugify.fullname" . }}
  labels:
    {{- include "kugify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kugify.selectorLabels" . | nindent 4 }}
